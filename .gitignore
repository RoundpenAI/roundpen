# Binaries
/bin/
/dist/
/roundpend
/roundpen

# Go
*.exe
*.test
*.out
vendor/

# Env / local data
.env
.env.local
/data/
*.db
.docker/

# Local Go build cache (never commit)
.gocache/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store

# pg0 / local postgres
.pg0/

# Embedded UI build output (keep .gitkeep so go:embed compiles)
/internal/ui/dist/*
!/internal/ui/dist/.gitkeep

# Browser / agent QEMU build artifacts (qcow2 + kernel sidecars)
/images/browser-qemu/out/
/images/agent-qemu/out/

# Node (web/)
/web/node_modules/
/web/dist/
/web/test-results/
/web/playwright-report/
/web/blob-report/
/web/playwright/.cache/
